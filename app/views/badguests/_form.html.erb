<%= form_for(badguest) do |f| %>
  <% if badguest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(badguest.errors.count, "error") %> prohibited this badguest from being saved:</h2>

      <ul>
      <% badguest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phone %>
    <%= f.text_field :phone %>
  </div>

  <div class="field">
    <%= f.label :desc %>
    <%= f.text_field :desc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
